/**
 * Multiplie un nombre par deux.
 * @param value Le nombre à multiplier.
 * @returns Le résultat de la multiplication.
 */
export function by2(value: number): number {
  return value * 2;
}

export function tutu (){
  return 'tutu ZZ';
}